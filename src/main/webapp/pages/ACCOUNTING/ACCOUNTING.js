angular.module('ACCOUNT').controller('accountingController',
		[ '$scope', '$rootElement', '$http', '$location', function($scope, $rootElement, $http, $location) {
			$scope.textMessage = 'accountingController';
			

		} ]);